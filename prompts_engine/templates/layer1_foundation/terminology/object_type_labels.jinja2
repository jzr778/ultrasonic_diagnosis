#### 障碍物类型标签（训练 / 标注）

超声附近真实障碍的类型判定使用下列英文标签（**勿使用已废弃的 curb_like**）：

| 标签 | 含义 |
|------|------|
| `parking_curb` | **车位路沿**：车位开口边界路沿，路沿内侧/上方是泊入区，泊入时**可压过** |
| `hard_curb` | **硬路沿/台阶**：车道边、人行道边、墙根台阶等，**不可压过、不可越** |
| `wheel_stop` | 轮挡/限位器（车位内部） |
| `speed_bump` | 减速带 |
| `ground_irregularity` | 地面异常（井盖、凹凸、小坑等） |
| `other_obstacle` | 墙、车、柱、人等其余障碍 |

**区分路沿**：优先看路沿**另一侧是否为要泊入的车位**——是则 `parking_curb`，否则 `hard_curb`。
