{% if yellow_freespace %}
本帧图中绘制的**黄色边缘线**对应相机感知 **PARK_FREESPACE** 障碍。下列为每条已绘制黄线的 **freespaceType**（规范化枚举名）及其在 AVM 上的**质心像素坐标** `[u, v]`（图像 640×800，原点左上）：

{% for item in yellow_freespace %}
- 第 {{ loop.index }} 条：freespaceType = **{{ item.freespaceType }}**（{{ item.freespaceTypeZh }}），质心 [u, v] = **[{{ item.centroid[0] }}, {{ item.centroid[1] }}]**
{% endfor %}

请注意：根据黄色质心像素坐标在图像中找到对应的黄线，再结合 freespaceType 判断障碍物的类型；**最终要以 AVM（及必要时鱼眼）中可见的真实相机障碍实体为准**，黄线用于辅助对齐该实体，而非替代对实物的辨认。**不能根据超声障碍与相机障碍的质心判断，仅依靠视觉能力判断是否误检，像素坐标仅用于辅助大模型定位图中的红色、黄色标记！**

{% else %}
本帧**未**传入相机障碍（PARK_FREESPACE）的 **freespaceType** 与黄色边缘线**质心坐标**结构化列表（可能因距离筛选等无条目）。**因此，本张 AVM 鸟瞰图在各红色超声标记周围没有按相机感知叠绘黄色边缘线**；请勿假设存在与输入对齐的黄线—红色标记对应关系。

**分析要求**：请**完全依据 AVM（及必要时鱼眼）真实像素**判断：只在**紧贴各红色超声标记质心及其极邻近区域**内，辨认是否存在墙、柱、邻车、路沿等**真实障碍**。**禁止**抛开红点去关联画面中远处或与红点之间有明显空旷地面间隔的物体；真伪结论必须锚定在红点局部，**不要偏离红色超声标记**。
{% endif %}
