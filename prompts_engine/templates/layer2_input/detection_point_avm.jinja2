红色超声标记质心以像素坐标形式提供，**AVM 图**大小为 **640×800**，图像左上角为原点 (0,0)。**请务必根据给定的质心像素坐标，精确定位红色超声标记在 AVM 中的实际位置，并结合图像内容进行判断**。
**重要：像素坐标是定位的重要可信依据，每个坐标对应一处红色超声标记质心，必须基于坐标在图上精确找到该红色超声标记再分析。不能根据超声障碍与相机障碍的质心判断，仅依靠视觉能力判断是否误检，像素坐标仅用于辅助大模型定位图中的红色、黄色标记！**
- **AVM** 上红色超声标记质心像素坐标：{{ avm }}
{% if vlm_yuyan_image_included %}
- **鱼眼**：不提供与红色标记配对的鱼眼像素坐标；请在 AVM 精确定位红色标记，并用语义上对照同路鱼眼中的**实物障碍**（绿/黄叠画区域对应的真实场景）。
{% endif %}