检测点以像素坐标形式提供，**AVM 图**大小为 **640×800**，图像左上角为原点 (0,0)，检测点像素坐标是**红色超声波障碍标记的质心，请务必根据给定的像素坐标，精确定位红色标记在 AVM 中的实际位置，并结合图像内容进行判断**。
**重要：像素坐标是定位红色标记的重要可信依据，一个像素坐标代表一个红色超声标记的质心，必须基于坐标找到红色超声标记进行精确分析**。
- **AVM** 上检测点像素坐标：{{ avm }}
{% if vlm_yuyan_image_included and yuyan_fish_points %}
- **鱼眼图**（1920×1440）上与超声对应的参考像素坐标（辅助定位，**勿与 AVM 坐标混用**）：{{ yuyan_fish_points }}
{% endif %}